class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        b=int(''.join(map(str, digits)))
        b+=1
        c=str(b)
        d=[]
        for digit in c:
            d.append (int(digit))
        return d
